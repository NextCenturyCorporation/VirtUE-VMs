Comment out handle_iptable in /etc/xen/scripts/vif-bridge

Also:
cd /etc
sudo patch -p1 < $CURDIR/sudoers.patch
sudo cp $CURDIR/local.conf /etc/ld.so.conf.d/
sudo ldconfig

After that:
Copy start-up scripts from config
