1. Unpack linux 4.18.20

2. Apply the PIE patch found on the kernel-hardening mailing list
(the patch is slighly modified to NOT enable PIE but some changes needed
for our PIC changes):

pie-patch-mailing-list.patch

3. Apply our PIC modules patch on top of it:

pic-modules-support.patch

For ENA module re-randomization support, apply further on top of it:

kaslr.patch

4. Use the kernel configuration (Dom0):

config-aws
